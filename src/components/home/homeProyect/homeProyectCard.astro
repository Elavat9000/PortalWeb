---
import { HiArrowRight } from "react-icons/hi";

export interface Props {
  titulo: string;
  imagen: string;
  tecnologias: string[];
  demo: string;
  codigo: string;
}

const { titulo, imagen, tecnologias, demo, codigo } = Astro.props;
---

<div
  class="group flex w-full max-w-md mx-auto flex-col overflow-hidden rounded-xl bg-red-950 border border-none transition-all hover:scale-[1.015] drop-shadow-[4px_4px_0_#770d10]"
>
  <img
    src={imagen}
    alt={`Proyecto: ${titulo}`}
    class="h-52 p-3 w-full object-cover rounded-2xl"
  />

  <div class="flex flex-col grow p-3">
    <h3 class="text-lg font-semibold mb-2 text-white drop-shadow-[1px_1px_0_#770d10]">{titulo}</h3>

    <div class="flex flex-wrap gap-2 mb-4">
      {
        tecnologias.map((tech) => (
          <span class="rounded-md bg-[#770d10]/20 text-[#a27ed1] px-2 py-0.5 text-xs font-semibold">
            {tech}
          </span>
        ))
      }
    </div>

    <div class="mt-auto flex justify-between gap-3">
      <a
        href={demo}
        target="_blank"
        rel="noopener noreferrer"
        class="w-full text-center border border-[#770d10] text-[#770d10] py-1.5 rounded-md text-sm font-semibold hover:bg-[#770d10] hover:text-gray-900 transition-colors"
      >
        Demo
      </a>

      <a
        href={codigo}
        target="_blank"
        rel="noopener noreferrer"
        class="w-full text-center border border-[#770d10] text-[#770d10] py-1.5 rounded-md text-sm font-semibold hover:bg-[#770d10] hover:text-gray-900 transition-colors"
      >
        Código
      </a>
    </div>
  </div>
</div>
